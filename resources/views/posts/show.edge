@layout('main')

@section('content')
  <a href="/posts">Go Back</a>
  <hr>

  <h1>{{ post.title }}</h1>
  <p>{{ post.body }}</p>

  <br>

  <a href="/posts/edit/{{ post.id }}" class="btn btn-dark d-block float-left">Edit Post</a>

  <form action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="post">
    {{ csrfField() }}
    <button type="submit" class="btn btn-danger margin-left-2">Delete</button>
  </form>

@endsection


